1. 找到页面创建完成的位置：在`create_full_widgets()`方法末尾，所有界面组件创建完成后
2. 添加自动刷新设备的代码：使用`self.root.after(500, self.async_refresh_devices)`延迟调用异步刷新方法
3. 选择异步刷新方法的原因：`async_refresh_devices()`会在后台线程执行，不会阻塞界面，提供更好的用户体验
4. 代码位置：在\`gui.py

