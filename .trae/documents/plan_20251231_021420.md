1. **实现启动后默认点击配置按钮**：

   * 在create\_full\_widgets方法的末尾添加代码，确保界面完全创建后点击配置按钮

   * 或者在async\_initialization方法中，在create\_full\_widgets调用后延迟执行点击操作

   * 确保toggle\_config\_btn已经创建，避免AttributeError

2. **实现步骤**：

   * 首先在create\_full\_widgets方法的末尾添加代码，调用self.toggle\_config()方法

   * 或者使用self.root.after()延迟执行点击操作

   * 确保配置按钮已经存在，避免出现AttributeError

3. **预期效果**：

   * 应用程序启动后，配置区域会自动展开

   * 无需用户手动点击配置按钮

   * 界面显示更加完整，方便用户查看和修改配置

