1. **默认收缩配置参数**：这个功能已经实现，因为我们之前设置了`self.config_collapsed = True`并调用了`self.toggle_config()`，所以配置区域默认是收缩的。

2. **重新组织按钮布局**：

   * 将收缩配置按钮从配置区域移到辅助功能按钮区域

   * 将AI润色按钮和历史记录按钮从任务输入框区域移到辅助功能按钮区域

   * 确保所有按钮都在加载配置按钮的右边

   * 保持按钮之间的合理间距和美观布局

3. **实现步骤**：

   * 首先移除配置区域中的收缩按钮

   * 然后移除任务输入框区域中的AI润色按钮和历史记录按钮

   * 最后在辅助功能按钮区域的加载配置按钮右边添加这三个按钮

4. **注意事项**：

   * 确保所有按钮的功能保持不变

   * 保持界面的美观和易用性

   * 确保按钮的顺序合理，符合用户使用习惯

5. **预期效果**：

   * 配置区域默认收缩，只显示基本配置项

   * 收缩配置按钮、AI润色按钮和历史记录按钮都位于辅助功能按钮区域，在加载配置按钮的右边

   * 界面更加简洁、紧凑，按钮布局更加合理

